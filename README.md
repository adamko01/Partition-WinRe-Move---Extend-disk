# Partition-Move---Extend-disk
PowerShell GUI for extending C: when free partition is not next to it, but the WinRE partition is next, with guided and quick workflows, BitLocker checks, status summaries, and logging.
